language: cpp

install: 
  - sudo add-apt-repository -y ppa:zoogie/sdl2-snapshots
  - sudo apt-get update
  - sudo apt-get install libsdl2-dev

script:
  - mkdir build
  - cd build
  - cmake ..
  - make -j3